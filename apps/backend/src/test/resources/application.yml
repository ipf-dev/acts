spring:
  datasource:
    url: jdbc:h2:mem:acts;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

acts:
  storage:
    bucket: acts-assets-test
    region: ap-northeast-2
