spring:
  datasource:
    url: jdbc:tc:postgresql:16:///acts
    username: acts
    password: acts
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

acts:
  auth:
    allowed-domains:
      - example.test
  storage:
    bucket: acts-assets-test
    region: ap-northeast-2
